---
title: "الخطوات الأولى"
description: "أنشئ agentic app تبدأ بالدردشة، أضف action، اعرض النتائج المنظمة داخل الدردشة، ثم وسعها إلى صفحة دائمة يستطيع agent فتحها."
---

# الخطوات الأولى

Agent-Native مخصص لبناء agentic applications: يشترك AI agent وواجهة UI في نفس [actions](/docs/actions) وبيانات SQL و application state. المسار الأساسي يبدأ من Chat app كي يستطيع المستخدمون التحدث مع agent فورًا، ثم يضيف أسطح التطبيق التي يحتاجها سير العمل.

المسار المفيد:

1. إنشاء Chat app.
2. إضافة action.
3. عرض نتيجة action داخل الدردشة كواجهة أصلية.
4. حفظ البيانات في SQL.
5. إضافة صفحة يستطيع agent فتحها عندما يكون الفحص البصري أنسب.

## 1. أنشئ Chat app {#create-your-app}

تحتاج إلى [Node.js 22+](https://nodejs.org) و[pnpm](https://pnpm.io).

```bash
npx @agent-native/core@latest create my-app --template chat
cd my-app
pnpm install
pnpm dev
```

يتضمن القالب durable chat threads و auth و live sync ومجلد `actions/` و actions القياسية `view-screen` و `navigate` وتطبيق React يمكن توسيعه.

## 2. أضف action {#add-an-action}

Action هي عملية typed operation مشتركة بين agent و UI. في هذا المسار أنشئ `actions/analyze-responses.ts`: تحلل ردود النماذج وتعيد Response insights.

```ts
export default defineAction({
  description: "Analyze recent form responses and summarize the trend.",
  outputSchema: dataInsightsWidgetResultSchema,
  chatUI: {
    renderer: ACTION_CHAT_UI_DATA_INSIGHTS_RENDERER,
    title: "Response insights",
  },
  readOnly: true,
  run: async () =>
    createDataInsightsWidgetResult({
      title: "Response insights",
      display: {
        title: "42 responses",
        description: "Completion rose this week.",
        primaryAction: {
          label: "افتح رؤى الردود",
          href: "/response-insights",
        },
      },
      chartSeries: {
        type: "bar",
        xKey: "day",
        series: [{ key: "responses", label: "Responses" }],
        data: Array.from({ length: 10 }, (_, day) => ({
          day: day + 1,
          responses: Math.round(Math.random() * 10) + 10,
        })),
      },
    }),
});
```

شغلها مباشرة:

```bash
pnpm action analyze-responses --formId demo
```

أو اسأل agent في المتصفح:

> حلّل الردود الأخيرة لنموذج العرض.

## 3. اعرض النتيجة داخل الدردشة {#render-inline}

لأن action تعلن `outputSchema` و `chatUI.renderer`، لا يحول transcript البيانات المنظمة إلى نص عادي. يعرض مكون React أصليًا: ملخصًا ورسمًا وجدولًا ورابطًا إلى الصفحة التفصيلية. راجع [Native Chat UI](/docs/native-chat-ui).

لإنشاء رسمك الخاص بدل استخدام data widgets المدمجة، أعط action معرّف renderer خاصًا بالمنتج وأعد شكل JSON الخاص بك بعد التحقق منه:

```ts
// actions/analyze-responses.ts
import { defineAction } from "@agent-native/core/action";
import { z } from "zod";

const responseChartRenderer = "responses.response-chart";

const responseChartResultSchema = z.object({
  title: z.string(),
  points: z.array(
    z.object({
      day: z.string(),
      responses: z.number(),
    }),
  ),
});

export default defineAction({
  description: "Analyze recent form responses and render a custom chart.",
  schema: z.object({
    formId: z.string().default("demo"),
  }),
  outputSchema: responseChartResultSchema,
  chatUI: {
    renderer: responseChartRenderer,
    title: "Response chart",
  },
  readOnly: true,
  run: async ({ formId }) => ({
    title: `Responses for ${formId}`,
    points: [
      { day: "Mon", responses: 12 },
      { day: "Tue", responses: 18 },
      { day: "Wed", responses: 24 },
    ],
  }),
});
```

ثم سجّل مكون React لذلك المعرّف نفسه عند بدء التطبيق، مثل استيراد `app/chat-renderers.tsx` مرة واحدة من `app/root.tsx`:

```tsx
// app/chat-renderers.tsx
import {
  registerActionChatRenderer,
  type ToolRendererProps,
} from "@agent-native/core/client/chat";

type ResponseChartResult = {
  title: string;
  points: Array<{ day: string; responses: number }>;
};

function ResponseChart({ context }: ToolRendererProps) {
  const result = context.resultJson as ResponseChartResult;
  const max = Math.max(...result.points.map((point) => point.responses), 1);

  return (
    <section className="rounded-lg border bg-card p-4">
      <h3 className="text-sm font-medium">{result.title}</h3>
      <div className="mt-4 flex h-32 items-end gap-2">
        {result.points.map((point) => (
          <div
            key={point.day}
            className="flex flex-1 flex-col items-center gap-2"
          >
            <div
              className="w-full rounded-t bg-primary"
              style={{ height: `${(point.responses / max) * 100}%` }}
            />
            <span className="text-xs text-muted-foreground">{point.day}</span>
          </div>
        ))}
      </div>
    </section>
  );
}

registerActionChatRenderer({
  id: "responses.response-chart",
  renderer: "responses.response-chart",
  Component: ResponseChart,
});
```

<WireframeBlock id="doc-block-inline-result-wireframe-ar">
  <Screen
    surface="desktop"
    html={
      "<div style='min-height:520px;box-sizing:border-box;padding:36px;display:flex;justify-content:center;background:var(--wf-bg)'><div style='width:min(780px,100%);display:flex;flex-direction:column;gap:18px'><div style='align-self:flex-end;max-width:70%' class='wf-card' data-rough><strong>User</strong><p style='margin:8px 0 0'>حلّل الردود الأخيرة لنموذج العرض.</p></div><div style='align-self:flex-start;width:min(620px,100%)' class='wf-card' data-rough><strong>Agent</strong><p class='wf-muted' style='margin:8px 0 14px'>إليك أحدث رؤى الردود.</p><div class='wf-card' data-rough style='display:grid;grid-template-columns:1.15fr .85fr;gap:14px'><div><div style='display:flex;align-items:baseline;justify-content:space-between'><strong>42 responses</strong><span class='wf-pill accent' data-rough>+18%</span></div><div data-rough='line:bottom' style='height:120px;margin-top:16px;display:flex;align-items:end;gap:10px;border-bottom:1.4px solid var(--wf-line);padding-bottom:4px'><div data-rough style='height:46%;flex:1;background:color-mix(in srgb, var(--wf-accent) 18%, transparent);border:1.4px solid var(--wf-accent);border-radius:8px'></div><div data-rough style='height:72%;flex:1;background:color-mix(in srgb, var(--wf-accent) 28%, transparent);border:1.4px solid var(--wf-accent);border-radius:8px'></div><div data-rough style='height:94%;flex:1;background:color-mix(in srgb, var(--wf-accent) 42%, transparent);border:1.4px solid var(--wf-accent);border-radius:8px'></div></div></div><div><strong>أهم الإجابات</strong><div style='margin-top:14px;display:flex;flex-direction:column;gap:8px'><div class='wf-row wf-box' data-rough style='display:flex;justify-content:space-between;gap:12px;padding:6px 10px'><span>Yes</span><span>31</span></div><div class='wf-row wf-box' data-rough style='display:flex;justify-content:space-between;gap:12px;padding:6px 10px'><span>No</span><span>11</span></div></div><button class='primary' data-rough style='margin-top:16px;width:100%'>افتح رؤى الردود</button></div></div></div></div></div>"
    }
  />
</WireframeBlock>

## 4. احفظ البيانات في SQL {#persist-data}

عندما تحتاج النتيجة إلى المراجعة أو المقارنة أو التحديث، احفظها في SQL واجعل القراءة والكتابة خلف actions:

- `create-response-insight` يكتب insight جديدًا.
- `list-response-insights` يقرأ بيانات الصفحة.
- `update-response-insight` يحدث السجل.
- `analyze-responses` يستمر في إعادة widget الدردشة الأصلي.

لا تنشئ REST route مكررة للمتصفح فقط؛ يجب أن تشترك UI و agent في actions.

## 5. أضف صفحة يستطيع agent فتحها {#add-a-page}

أضف route باسم `/response-insights` واستخدم `useActionQuery("list-response-insights")` و `useActionMutation("create-response-insight")`. هذه الصفحة عرض لحالة SQL؛ وتظل البيانات مكتوبة عبر نفس actions.

وسع `view-screen` و `navigate` كي يعرف agent المسار الحالي ويستطيع فتح `/response-insights`. يمكن للنتائج أيضًا تضمين `href: "/response-insights"` لعرض زر "افتح رؤى الردود" في الدردشة.

عندما تفتح الدردشة الكاملة صفحة تطبيق، استخدم `AgentChatSurface` و `AgentSidebar` و `useAgentChatHomeHandoff` و `useAgentChatHomeHandoffLinks` و `chatViewTransition` من [Agent Surfaces](/docs/agent-surfaces#rich-chat) كي تنتقل الدردشة إلى اللوحة الجانبية مع الحفاظ على نفس thread.

## بنية المشروع {#project-structure}

```text
my-app/
  actions/         # عمليات يمكن للوكيل وواجهة المستخدم استدعاؤها
  app/             # مسارات React وصفحاته وواجهات الدردشة
  server/          # خادم Nitro ومخطط SQL
  AGENTS.md        # تعليمات دائمة لوكيل التطبيق
  .agents/         # المهارات التي يحمّلها الوكيل عند الحاجة
  data/app.db      # حالة SQLite المحلية عند عدم ضبط DATABASE_URL
```

## هل تريد نقطة بداية كاملة للتحليلات؟ {#analytics-starting-point}

مثال Response insights أعلاه صغير عمدًا كي ترى أجزاء framework بوضوح. إذا كنت تبني منتج analytics حقيقيًا، فابدأ من [Analytics](/docs/template-analytics). إنه نقطة بداية قوية: صل providers الخاصة بك، استخدم dashboards و agent actions الموجودة، ثم خصص التطبيق من هناك.

## الخطوات التالية {#next}

- **[Chat Template](/docs/template-chat)** — أصغر chat-first app.
- **[Actions](/docs/actions)** — schemas و auth و approvals و hooks و transport.
- **[Native Chat UI](/docs/native-chat-ui)** — عرض نتائج actions كجداول ورسوم و typed cards.
- **[Analytics Template](/docs/template-analytics)** — نقطة بداية قوية لتطبيق analytics؛ صل providers ثم خصص من هناك.
- **[Context Awareness](/docs/context-awareness)** — `view-screen` و `navigate` و route state والعناصر المحددة.
- **[Agent Surfaces](/docs/agent-surfaces)** — chat و inline UI و app pages و embedded sidecars و automation و agents الخارجية.
