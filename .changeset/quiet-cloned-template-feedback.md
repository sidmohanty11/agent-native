---
"@agent-native/core": patch
"@agent-native/dispatch": patch
---

Make template feedback controls opt in through `VITE_AGENT_NATIVE_FEEDBACK_URL` so cloned apps do not send feedback to Agent Native by default.
