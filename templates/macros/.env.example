ANTHROPIC_API_KEY=
# Set real values only in your local .env or deployment secrets. Never commit
# real API keys, tokens, webhook URLs, signing secrets, or private data.
# Optional: Set DATABASE_URL to use Postgres instead of SQLite
# DATABASE_URL=postgresql://postgres:password@host:5432/postgres

# Supabase — required for auth in this template
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=
